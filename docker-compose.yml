services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /data/pyvd.db
      DOWNLOAD_DIR: /tmp/pyvd
    volumes:
      - db_data:/data
    ports:
      - "5868:5868"

volumes:
  db_data:
